name: domain
description: A new Flutter package project.
version: 0.0.1
homepage:

environment:
  sdk: '>=2.18.4 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  data:
    path: ../data
  core:
    path: ../core
  firebase_auth: ^3.2.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner: ^2.2.1
  injectable_generator: ^2.1.3
  smartstruct_generator: ^1.3.0

flutter:
